import LoginComponent from "../../components/LoginComponent/LoginComponent";

const LoginPage = () => {
    return (
        <div>
            <LoginComponent/>
        </div>
    );
};

export default LoginPage;